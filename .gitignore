# ---------------------------------------------------------------------------
# ResearchMind MCP — Git ignore rules
# Reflects the approved MVP stack: Python/Poetry backend, Next.js frontend,
# Docker/Compose infrastructure, Postgres + Qdrant + Redis services.
# ---------------------------------------------------------------------------

# --- Secrets & environment -------------------------------------------------
# Never commit real environment files. Examples are the only exception.
.env
.env.*
!.env.example
!frontend/.env.example
*.pem
*.key
*.p12
*.pfx
secrets/
credentials.json
service-account*.json

# --- Python ----------------------------------------------------------------
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
sdist/
wheels/
*.egg-info/
*.egg
MANIFEST
pip-log.txt
pip-delete-this-directory.txt

# --- Python virtual environments -------------------------------------------
.venv/
venv/
ENV/
env/
.python-version

# --- Test, type & lint caches ----------------------------------------------
.pytest_cache/
.mypy_cache/
.dmypy.json
dmypy.json
.ruff_cache/
.tox/
.nox/
.hypothesis/

# --- Coverage --------------------------------------------------------------
.coverage
.coverage.*
coverage.xml
htmlcov/
*.cover
nosetests.xml

# --- Node / Next.js (frontend) ---------------------------------------------
node_modules/
.next/
out/
.turbo/
.vercel/
*.tsbuildinfo
next-env.d.ts
.npm/
.yarn/
.pnp.*
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.eslintcache

# --- Docker / Compose runtime ----------------------------------------------
docker-compose.override.yml
.docker/

# --- Local service data volumes --------------------------------------------
# Postgres, Qdrant and Redis state must never be committed.
#
# Leading slashes are deliberate throughout this section and the two below. An
# unanchored `foo/` matches a directory named foo at ANY depth, so it silently
# swallows source that happens to share the name. That is not hypothetical here:
# a bare `models/` excluded the whole of shared/models/ — the domain model layer
# — from the repository, and it took promoting Backend CI in Sprint M0/S0.5 to
# notice. These volumes only ever exist at the repository root, so say so.
/data/
/pgdata/
/postgres_data/
/qdrant_data/
/qdrant_storage/
/redis_data/
dump.rdb
*.db
*.sqlite
*.sqlite3

# --- Application runtime artifacts -----------------------------------------
# Root-anchored: see the note in the service-volumes section above.
/uploads/
/storage/
/logs/
*.log
/tmp/
/temp/

# --- ML / embedding model caches -------------------------------------------
# FastEmbed (BAAI/bge-small-en-v1.5) downloads weights locally; they are baked
# into the image at build time and must never enter version control.
.fastembed_cache/
# Root-anchored. `models/` unanchored matched shared/models/ and removed the
# project's domain models from version control; see the note above.
/models/
*.onnx
*.safetensors
*.bin

# --- Evaluation outputs ----------------------------------------------------
# The golden dataset is committed; generated reports are not.
evaluation/results/
evaluation/*.report.json

# --- IDE / editor ----------------------------------------------------------
# .vscode/ is excluded wholesale: this tree contains a 1.1 GB VS Code C/C++
# IntelliSense database (browse.vc.db) left over from an unrelated ROS2
# workspace. It exceeds GitHub's 100 MB per-file limit and must never be staged.
.vscode/
.idea/
*.swp
*.swo
*~
.project
.pydevproject
.settings/

# --- OS --------------------------------------------------------------------
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
desktop.ini
