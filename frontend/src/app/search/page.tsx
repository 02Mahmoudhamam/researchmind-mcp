/**
 * Search Page
 * Semantic Search — search across the document corpus with filters
 * TODO: implement full UI
 */
"use client";

export default function SearchPage() {
  return (
    <main className="flex min-h-screen flex-col p-8">
      <h1 className="text-2xl font-semibold text-gray-900">Search</h1>
      <p className="mt-2 text-gray-500">Semantic Search — search across the document corpus with filters</p>
    </main>
  );
}
