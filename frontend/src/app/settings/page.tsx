/**
 * Settings Page
 * Settings — user profile, API keys, preferences, team management
 * TODO: implement full UI
 */
"use client";

export default function SettingsPage() {
  return (
    <main className="flex min-h-screen flex-col p-8">
      <h1 className="text-2xl font-semibold text-gray-900">Settings</h1>
      <p className="mt-2 text-gray-500">Settings — user profile, API keys, preferences, team management</p>
    </main>
  );
}
