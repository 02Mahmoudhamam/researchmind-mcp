/**
 * Upload Page
 * Upload Page — drag and drop PDF/document upload with processing status
 * TODO: implement full UI
 */
"use client";

export default function UploadPage() {
  return (
    <main className="flex min-h-screen flex-col p-8">
      <h1 className="text-2xl font-semibold text-gray-900">Upload</h1>
      <p className="mt-2 text-gray-500">Upload Page — drag and drop PDF/document upload with processing status</p>
    </main>
  );
}
