/**
 * Workspace Page
 * Research Workspace — chat with agents, view results, manage sessions
 * TODO: implement full UI
 */
"use client";

export default function WorkspacePage() {
  return (
    <main className="flex min-h-screen flex-col p-8">
      <h1 className="text-2xl font-semibold text-gray-900">Workspace</h1>
      <p className="mt-2 text-gray-500">Research Workspace — chat with agents, view results, manage sessions</p>
    </main>
  );
}
