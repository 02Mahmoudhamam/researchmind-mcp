/**
 * Dashboard Page
 * Research Dashboard — shows summary stats, recent documents, agent activity
 * TODO: implement full UI
 */
"use client";

export default function DashboardPage() {
  return (
    <main className="flex min-h-screen flex-col p-8">
      <h1 className="text-2xl font-semibold text-gray-900">Dashboard</h1>
      <p className="mt-2 text-gray-500">Research Dashboard — shows summary stats, recent documents, agent activity</p>
    </main>
  );
}
